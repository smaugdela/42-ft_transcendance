version: "3.1"

services:
  database:
    image: postgres:15
    container_name: postgres
    restart: always
    env_file:
      - ../backend/.env
    ports:
      - 5432:5432
    networks:
      - backend
  # adminer:
  #   image: adminer:4.8.1
  #   restart: always
  #   ports:
  #     - 8080:8080

networks:
  # frontend:
  #   driver: bridge
  backend:
    driver: bridge
